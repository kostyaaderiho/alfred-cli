/**
 * Contains default configurations for supported blueprints.
 *
 * In order to add new blueprint, blueprint's default config must be described there.
 *
 * name: blueprint name.
 * copyPath: target path where blueprint should be generated.
 */
module.exports = [
    {
        name: 'component',
        copyPath: 'src/components/'
    }
];
